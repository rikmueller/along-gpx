# AlongGPX Frontend Configuration
# This file configures Vite dev server for local development

# Hostname for Vite HMR (Hot Module Replacement) and allowedHosts
# Set this to your Docker host or external hostname for remote development
# ALONGGPX_HOSTNAME=alonggpx.emanri.de

# Backend URL for Vite proxy
# Only needed when backend is not on localhost:5000 (default)
# Docker deployments set this in docker-compose.yml instead
# VITE_BACKEND_URL=http://localhost:5000
